name: Keep Production Database Awake

on:
  schedule:
    - cron: "17 7 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Touch database health endpoint
        run: curl --fail --show-error --silent https://unique-college-api.onrender.com/api/health/db
